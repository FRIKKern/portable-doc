/**
 * @portable-doc/fixtures — canonical example documents for the AST.
 *
 * The .ts files are the source of truth (typed against PortableDoc); the
 * sibling .json files are emitted artifacts kept in sync with `pnpm
 * fixtures:emit`.
 */

export { welcome } from './welcome.js';
export { incident } from './incident.js';
