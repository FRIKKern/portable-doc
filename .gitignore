# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Node / build artefacts
node_modules/
dist/
coverage/
.turbo/
.vite/
*.tsbuildinfo

# OS / logs
.DS_Store
*.log

# Visual-goldens artifacts (regenerable via `pnpm visual-goldens`)
goldens/

# Structural-check harness output (regenerable via `pnpm -C apps/editor check:structural`)
.papir-check/
apps/editor/.papir-check/

# paperflow workflow state (per-instance pointers, not for distribution)
.paperflow/
