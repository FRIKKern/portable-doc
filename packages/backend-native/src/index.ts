/**
 * @portable-doc/backend-native — public API.
 *
 * Thin React component that mounts a Pd-tree as actual `react-native`
 * primitives (View / Text / Pressable / Image). For Expo / Metro consumers.
 */

export { PdRender, default } from './PdRender.js';
export type { PdRenderProps } from './PdRender.js';
