# playground

Public playground — paste a PortableDoc JSON, see it live-validated, then preview
it across all five surfaces (Web, Email, TUI, Native, Text) rendered by the same
kernel, and share via a `?doc=` URL. Deployed to GitHub Pages at
`https://frikkern.github.io/portable-doc/` (`vite.config.ts` base `/portable-doc/`).
